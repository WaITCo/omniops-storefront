services:
  storefront:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: omniops-storefront:latest
    ports:
      - "3002:3000"
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
